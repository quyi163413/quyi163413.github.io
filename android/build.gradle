buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // 可选国内镜像
        // maven { url 'https://maven.aliyun.com/repository/public' }
    }
}
